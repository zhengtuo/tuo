<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.zheng.learn_android">

    <permission android:name="com.aidl.use" />
    <!--网络状态权限-->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!--自定义权限-->
    <uses-permission android:name="com.aidl.use" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!--发送粘性广播需要此权限-->
    <uses-permission android:name="android.permission.BROADCAST_STICKY"/>
    <application
        android:name="app.LKApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.Light.NoActionBar">

        <activity
            android:name=".ui.interview.handler.HandlerActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".ui.interview.LearnService" />
        <!--广播-->
        <receiver
            android:name=".ui.interview.assembly.NetReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action
                    android:name="android.net.conn.CONNECTIVITY_CHANGE"
                    tools:ignore="BatteryLife" />
            </intent-filter>
        </receiver>
        <receiver android:name=".ui.interview.assembly.CustomizeReceiver"
            android:enabled="true"
            android:exported="true"
            tools:ignore="ExportedReceiver">
            <intent-filter>
                <action android:name="com.zheng.learn_android.CUSTOMIZE_BROADCAST"/>
            </intent-filter>
        </receiver>
        <receiver android:name=".ui.interview.assembly.Customize2Receiver"
            android:enabled="true"
            android:exported="true"
            tools:ignore="ExportedReceiver">
            <!--优先级-->
            <intent-filter android:priority="100">
                <action android:name="com.zheng.learn_android.CUSTOMIZE_BROADCAST"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>